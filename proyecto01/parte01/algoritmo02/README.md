# Algoritmo 02 - FP Growth
## Descripción
Por cada patrón elaboré un archivo con el código.

Cada patrón será explicado en el documento final.

## Bibliotecas
Instalar las siguientes bibliotecas

    install.packages("dplyr")
    install.packages("purrr")
    install.packages("readxl")
    install.packages("arules")
---

## Consideraciones
Tomar en cuenta que se necesita la biblioteca de fim4r y para esto es necesario instalar también rtools según la arquitectura de la computadora.
Puede variar la forma de instalación pero lo que me funcionó es que al momento de utilizar el algoritmo me preguntó si deseo instalar la biblioteca, respondí que sí y listo.

[Regresar](..)